extends ../layout/layout

block content
    h1#title Add a post
    form#addPost(action="/add", method='post')
        input(type="text", name="title", placeholder="title")
        input(type="date", name="date")
        input(type="submit", value="submit")